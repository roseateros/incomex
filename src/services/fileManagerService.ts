// Las funciones de gestión de archivos se retiraron del proyecto.
// Se mantiene este stub para evitar errores con imports antiguos que puedan persistir en la caché.
export const fileManagerService = {
  async notAvailable(): Promise<never> {
    throw new Error('La gestión de archivos está deshabilitada en esta versión.');
  },
};
